---
// src/components/NoteCard.astro
export interface NoteArticle {
  title: string;
  link: string;
  date: string;
  imgUrl: string;
  creator: string;
  labelColor?: string;
}

interface Props {
  article: NoteArticle;
}

const { article } = Astro.props;
const labelClass = article.labelColor || "bg-emerald-600 text-white";
---

<article class="bg-white rounded-3xl border border-slate-200 shadow-xs overflow-hidden flex flex-col group hover:shadow-md transition-shadow duration-300 relative">
  <span class={`absolute top-4 left-4 z-10 ${labelClass} text-[10px] font-black px-2.5 py-1 rounded-full uppercase tracking-wider shadow-sm`}>
    {article.creator}
  </span>
  <a href={article.link} target="_blank" rel="noopener noreferrer" class="flex flex-col h-full no-underline">
    <div class="aspect-video w-full bg-slate-100 overflow-hidden relative">
      <img
        src={article.imgUrl}
        alt={article.title}
        class="w-full h-full object-cover group-hover:scale-105 transition duration-500"
        loading="lazy"
        onerror="this.src='https://placehold.co/600x400/e2e8f0/475569?text=NOTE'; this.onerror=null;"
      />
    </div>
    <div class="p-5 sm:p-6 flex flex-col flex-grow justify-between text-left">
      <div class="space-y-2">
        <span class="text-xs font-bold text-slate-400 block font-mono">{article.date}</span>
        <h3 class="text-sm sm:text-base font-black text-slate-950 leading-snug group-hover:text-blue-600 transition-colors line-clamp-3">
          {article.title}
        </h3>
      </div>
      <span class="text-xs font-bold text-blue-600 flex items-center space-x-1 pt-4 mt-auto">
        <span>記事を読む</span>
        <svg class="w-3 h-3 transition-transform duration-200 group-hover:translate-x-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M9 5l7 7-7 7"></path>
        </svg>
      </span>
    </div>
  </a>
</article>
